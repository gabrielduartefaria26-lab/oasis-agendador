name: outliers oasis

on:
  schedule:
    - cron: "0 10 * * 1"   # segunda 07:00 em Brasilia
  workflow_dispatch:

permissions:
  contents: write

# grupo proprio: o plantao da publicacao fica ate 5h30 rodando e seguraria esta fila.
# So mexe no outliers.csv, e o pull --rebase antes do push resolve o resto.
concurrency:
  group: outliers-oasis
  cancel-in-progress: false

jobs:
  outliers:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: cacar Reels e carrosseis fora da curva
        env:
          IG_TOKEN: ${{ secrets.IG_TOKEN }}
          IG_USER_ID: ${{ secrets.IG_USER_ID }}
        run: python3 outliers.py

      - name: guardar
        run: |
          if [ -n "$(git status --porcelain outliers.csv outliers-carrossel.csv)" ]; then
            git config user.name "agendador"
            git config user.email "agendador@users.noreply.github.com"
            git add outliers.csv outliers-carrossel.csv
            git commit -m "outliers: $(date -u +%Y-%m-%d)"
            git pull --rebase --quiet origin main
            git push
          fi
